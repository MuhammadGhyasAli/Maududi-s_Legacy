[project]
name = "maududi-legacy-chat"
version = "0.1.0"
description = "Maududi's Legacy Chat API - Cloudflare Worker"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pydantic",
    "httpx"
]
